using UnityEngine;
using UnityEngine.UI;

public class SetObjectMenuToggleOnStart : MonoBehaviour
{

    public Toggle toggle;

    // Start is called before the first frame update
    void Start()
    {
        toggle.Select();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
